"""HERETIC — autonomous AI agent for business-logic vulnerability discovery.

A CLI tool that reasons about an application's business intent, then
systematically tries to violate the invariants it assumes but never enforces.

See docs/ for the full design. The moat is the Oracle (core/oracle.py).
"""

__version__ = "1.0.0"
