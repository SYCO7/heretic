# Bugs HERETIC should find in this crAPI run, as {bug_class, match} where `match`
# is a substring of the finding title.
#
# vehicle-location BOLA (both cross-read directions) + the bulk-exfiltration chain
# it composes into (2 BOLAs on the same endpoint => any user's GPS is readable).

ground_truth:
  - { bug_class: bola,  match: "vehicle_location" }        # userB reads userA's vehicle location
  - { bug_class: bola,  match: "vehicle_location" }        # userA reads userB's vehicle location
  - { bug_class: chain, match: "Bulk data exfiltration" }  # escalation: enumerate all users' locations
