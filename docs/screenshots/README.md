# PoC screenshots

Drop your engagement screenshots here for the README / LinkedIn post:

- `menu.png`      — `heretic menu` banner
- `findings.png`  — the confirmed-findings table (live crAPI run)
- `report.png`    — the scored HTML report open in a browser

These are safe to commit (no secrets). Redact any real hostnames/tokens first.
